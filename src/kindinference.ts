import { GTEnv } from './env';
import { Type } from './types';
import { Kind, eqKind, showKind, kType } from './kinds';
import { terr } from './util';

export const kindOf = (genv: GTEnv, t: Type): Kind => {
  // TODO: implement
  return kType;
};
